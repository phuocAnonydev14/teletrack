export enum AppTrackFeat {
  TOP_50 = 'top-50',
  COMPARE = 'compare',
  WATCHLIST = 'watchlist',
  SUBMIT = 'submit-app',
}

export enum HttpMethodEnum {
  GET = 'get',
  POST = 'post',
  PATCH = 'patch',
  PUT = 'put',
  DELETE = 'delete',
}

export enum PostEnum {
  DEFAULT = 'defaults',
  SOCIAL = 'socials',
}

export enum TokenEnum {
  ACCESS = 'access_token',
  REFRESH = 'refresh_token',
  USER = 'user',
}
